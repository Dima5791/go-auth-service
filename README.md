# Go Auth Service

Authentication & authorization microservice written in Go.

##  Features

- JWT authentication (access + refresh tokens)
- Refresh token rotation
- Role-based access control
- PostgreSQL storage
- Clean Architecture
- Docker & docker-compose
- SQL migrations

##  Project Structure

